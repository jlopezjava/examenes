package com.almundo.backend.app;

/**
 * Esta clase representa los estados
 * de la tarea.
 */
public enum Role {
	
	/** The employee. */
	EMPLOYEE,
	
	/** The supervisor. */
	SUPERVISOR,
	
	/** The director. */
	DIRECTOR,
	
	/** The on hold. */
	ON_HOLD;
}
